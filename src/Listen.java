
public class Listen extends Thread {

    Channel ch; //the channel running the listening thread

    public Listen(Channel ch) {
        this.ch = ch;
    }

    public void run() {
        while (true)
        {


        }
    }
}
